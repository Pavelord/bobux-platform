# Куда брать рисунки для отчета Bobux

Файл отчета: `C:\robloxclone\практика\Отчет_УТП_сетевой_модуль_Bobux_точная_копия_примера.docx`.

Готовые схемы лежат здесь: `C:\robloxclone\практика\готовые_рисунки_для_отчета`.

Как вставлять: в Word удаляешь серую заглушку `МЕСТО ДЛЯ РИСУНКА`, вставляешь нужный скрин или PNG над подписью `Рисунок N - ...`, подпись не удаляешь.

## Рисунок 1 - Общая структура сетевого модуля игровой платформы Bobux

Готовый файл: `C:\robloxclone\практика\готовые_рисунки_для_отчета\01_architecture_network_module.png`.

Это не скриншот из игры, а схема. Ее нужно вставить как готовую картинку.

## Рисунок 2 - Структура ролей пользователей и серверных компонентов

Готовый файл: `C:\robloxclone\практика\готовые_рисунки_для_отчета\02_roles_network_module.png`.

Это тоже схема, а не скриншот из приложения.

## Рисунок 3 - Клиентский модуль сетевого взаимодействия в проекте Godot

Скриншот кода: `C:\robloxclone\autoload\cloud_api.gd`, строки примерно `18-55`.

На скрине должны быть видны комментарий `PRACTICE SCREENSHOT: Cloud API configuration`, пути сессии и сетевые настройки.

## Рисунок 4 - Главное меню платформы Bobux после авторизации

Скриншот приложения: запусти Bobux, войди в аккаунт, дождись главного меню.

Нужно, чтобы на скрине были видны верхнее меню, боковая навигация и домашний экран.

## Рисунок 5 - Загрузка опубликованных режимов из серверной базы

Скриншот приложения: открой вкладку `Games` или блок с рекомендованными режимами в главном меню.

Нужно показать карточки режимов, названия игр и изображения/превью режимов.

## Рисунок 6 - Форма авторизации пользователя

Скриншот приложения до входа в аккаунт.

Нужно показать поля логина/пароля и кнопку входа.

## Рисунок 7 - Серверные маршруты регистрации и авторизации

Скриншот кода: `C:\robloxclone\services\bobux_api\server.js`, строки примерно `147-205`.

На скрине должны быть видны маршруты `/api/auth/v1/signup` и `/api/auth/v1/token`.

## Рисунок 8 - Клиентские функции регистрации и входа

Скриншот кода: `C:\robloxclone\autoload\cloud_api.gd`, строки примерно `251-320`.

На скрине должны быть видны функции `sign_up_with_credentials` и `sign_in_with_credentials`.

## Рисунок 9 - Сохранение сетевой сессии после авторизации

Скриншот кода: `C:\robloxclone\autoload\cloud_api.gd`, строки примерно `137-248`.

На скрине должны быть видны загрузка сохраненной сессии, `ensure_authenticated_session` и логика восстановления авторизации.

## Рисунок 10 - Отображение пользовательского контента в интерфейсе клиента

Скриншот приложения: открой `Catalog`, `Inventory` или `Avatar`.

Лучше показать раздел, где видны созданные предметы, каталог или пользовательские элементы.

## Рисунок 11 - Серверная обработка публикации пользовательского контента

Скриншот кода: `C:\robloxclone\services\bobux_api\server.js`, строки примерно `378-453`.

На скрине должны быть видны маршруты `publish_model_asset` и `publish_avatar_item`.

## Рисунок 12 - Серверное хранение изображений и файлов пользовательского контента

Скриншот кода: `C:\robloxclone\services\bobux_api\server.js`, строки примерно `668-684`.

Можно также рядом сделать второй скрин из `C:\robloxclone\autoload\cloud_api.gd`, строки `454-528`, где клиент отправляет файлы на сервер.

## Рисунок 13 - Подключение клиента к игровой комнате

Скриншот кода: `C:\robloxclone\autoload\client_network.gd`, строки примерно `1024-1110`.

На скрине должен быть виден комментарий `WebSocket client connection` и создание подключения к серверу.

## Рисунок 14 - Heartbeat активного игрового сервера

Скриншот кода: `C:\robloxclone\server\server_main.gd`, строки примерно `227-330`.

На скрине должен быть виден комментарий `Server heartbeat` и формирование данных активной комнаты.

## Рисунок 15 - Подтверждение входа игрока на выделенный сервер

Скриншот кода: `C:\robloxclone\autoload\client_network.gd`, строки примерно `1427-1470`.

На скрине должны быть видны этапы подтверждения комнаты и финального входа игрока.

## Рисунок 16 - Статистика базы данных проекта Bobux

Скриншот браузера или PocketBase.

Лучший вариант: открыть админку базы и показать коллекции `users`, `profiles`, `maps`, `catalog_items`, `active_servers`.

Если удобнее через API, можно открыть страницу/ответ статистики админ-модуля и показать числа пользователей, карт, предметов и активных серверов. Не вставляй в отчет пароли, токены и секретные ключи.

## Рисунок 17 - Формирование административной статистики

Скриншот кода: `C:\robloxclone\services\bobux_api\server.js`, строки примерно `1004-1076`.

На скрине должна быть видна функция `buildAdminStats`, которая собирает пользователей, карты, серверы, каталог и дружбы.

## Рисунок 18 - Универсальный HTTP-транспорт сетевого модуля

Скриншот кода: `C:\robloxclone\autoload\cloud_api.gd`, строки примерно `2301-2365`.

На скрине должна быть видна функция `_request_http_json`.

## Рисунок 19 - Оптимизация отображения списка друзей

Скриншот кода: `C:\robloxclone\scripts\lobby\friends_builder.gd`, строки примерно `815-845`.

На скрине должно быть видно, что вместо тяжелого 3D-превью используется легкий бейдж `_create_lightweight_avatar_badge`.

## Рисунок 20 - Проверка загрузки интерфейса после входа пользователя

Скриншот приложения: войди в аккаунт `pavelord` или тестовый аккаунт и покажи, что главное меню загрузилось, друзья/разделы не блокируют интерфейс.

Можно сделать скрин главного меню после входа, где видна навигация и загруженный профиль.

## Быстрый порядок работы

1. Сначала вставь готовые PNG для рисунков 1 и 2.
2. Затем сделай скриншоты приложения для рисунков 4, 5, 6, 10, 16, 20.
3. Потом открой указанные файлы кода и сделай скриншоты строк для рисунков 3, 7, 8, 9, 11, 12, 13, 14, 15, 17, 18, 19.
4. После вставки каждого изображения оставляй подпись под ним без изменений.
